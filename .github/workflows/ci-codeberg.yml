name: CI (Codeberg)
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    runs-on: docker://ghcr.io/cirruslabs/flutter:3.41.2
    steps:
      - uses: actions/checkout@v4
      - run: flutter pub get
      - run: dart analyze lib/
      - run: flutter test
